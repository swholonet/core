<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Star Wars Universe - Sicherheitscode zurücksetzen</title>
    <style>
        /* Reset styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Courier New', 'Monaco', 'Lucida Console', monospace;
            background: #000408;
            color: #00ffff;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: linear-gradient(135deg, #000408 0%, #000812 50%, #000204 100%);
            border: 2px solid #ff6600;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 0 30px rgba(255, 102, 0, 0.3);
        }

        .header {
            background: linear-gradient(90deg, #331100 0%, #442200 50%, #331100 100%);
            border-bottom: 2px solid #ff6600;
            padding: 30px 20px;
            text-align: center;
            position: relative;
        }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, #ff6600, transparent);
        }

        .title {
            font-size: 28px;
            font-weight: bold;
            text-shadow: 0 0 10px #ff6600;
            letter-spacing: 3px;
            margin-bottom: 10px;
            color: #ff6600;
        }

        .subtitle {
            font-size: 14px;
            color: #ffaa66;
            letter-spacing: 2px;
            font-weight: normal;
        }

        .security-notice {
            background: linear-gradient(135deg, #330000 0%, #440000 100%);
            border: 2px solid #ff3333;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
            text-align: center;
        }

        .security-icon {
            font-size: 24px;
            color: #ff3333;
            margin-bottom: 10px;
        }

        .security-text {
            font-size: 12px;
            color: #ffcccc;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .content {
            padding: 40px 30px;
        }

        .greeting {
            text-align: center;
            margin-bottom: 30px;
        }

        .commander-name {
            font-size: 20px;
            color: #ff6600;
            font-weight: bold;
            text-shadow: 0 0 5px #ff6600;
            margin: 10px 0;
        }

        .divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #ff6600, transparent);
            margin: 30px 0;
        }

        .message-section {
            background: linear-gradient(135deg, #221100 0%, #332200 100%);
            border: 1px solid #ff6600;
            border-radius: 8px;
            padding: 25px;
            margin: 20px 0;
        }

        .message-text {
            color: #ffddaa;
            font-size: 14px;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .reset-section {
            background: linear-gradient(135deg, #001122 0%, #002244 100%);
            border: 2px solid #00aaff;
            border-radius: 8px;
            padding: 30px;
            margin: 30px 0;
            text-align: center;
        }

        .reset-title {
            font-size: 16px;
            color: #00aaff;
            margin-bottom: 20px;
            letter-spacing: 1px;
        }

        .reset-button {
            display: inline-block;
            background: linear-gradient(135deg, #ff6600 0%, #cc5500 100%);
            color: #000;
            font-family: 'Courier New', monospace;
            font-size: 16px;
            font-weight: bold;
            text-decoration: none;
            padding: 15px 30px;
            border: 2px solid #ff6600;
            border-radius: 6px;
            letter-spacing: 1px;
            text-transform: uppercase;
            box-shadow: 0 0 20px rgba(255, 102, 0, 0.4);
            transition: all 0.3s ease;
            margin: 10px 0;
        }

        .reset-button:hover {
            background: linear-gradient(135deg, #ff8833 0%, #ee6622 100%);
            box-shadow: 0 0 25px rgba(255, 102, 0, 0.6);
        }

        .token-display {
            background: #003366;
            color: #00ffff;
            font-family: 'Courier New', monospace;
            font-size: 14px;
            padding: 15px;
            border: 1px dashed #00aaff;
            border-radius: 4px;
            margin: 20px 0;
            word-break: break-all;
            letter-spacing: 1px;
        }

        .warning-section {
            background: linear-gradient(135deg, #330000 0%, #220000 100%);
            border: 2px solid #ff3333;
            border-radius: 8px;
            padding: 20px;
            margin: 30px 0;
            text-align: center;
        }

        .warning-title {
            color: #ff3333;
            font-size: 14px;
            font-weight: bold;
            margin-bottom: 15px;
            letter-spacing: 1px;
        }

        .warning-text {
            color: #ffaaaa;
            font-size: 12px;
            line-height: 1.5;
        }

        .timer-section {
            background: linear-gradient(135deg, #1a1a00 0%, #2a2a00 100%);
            border: 1px solid #ffcc00;
            border-radius: 6px;
            padding: 20px;
            margin: 25px 0;
            text-align: center;
        }

        .timer-text {
            color: #ffcc00;
            font-size: 14px;
            font-weight: bold;
        }

        .timer-detail {
            color: #ffddaa;
            font-size: 12px;
            margin-top: 5px;
        }

        .footer {
            background: #001122;
            border-top: 1px solid #004444;
            padding: 25px;
            text-align: center;
            font-size: 12px;
            color: #666666;
        }

        .footer-links {
            margin-bottom: 15px;
        }

        .footer-link {
            color: #00aaaa;
            text-decoration: none;
            margin: 0 15px;
        }

        .footer-link:hover {
            color: #00ffff;
        }

        .terminal-border {
            border: 1px dashed #ff6600;
            background: rgba(255, 102, 0, 0.05);
            padding: 15px;
            margin: 20px 0;
            border-radius: 4px;
        }

        /* Mobile responsive */
        @media (max-width: 600px) {
            .container {
                margin: 10px;
                border-radius: 8px;
            }

            .content {
                padding: 20px 15px;
            }

            .reset-button {
                font-size: 14px;
                padding: 12px 24px;
            }

            .title {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="title">STAR WARS</div>
            <div class="subtitle">UNIVERSE</div>

            <div class="security-notice">
                <div class="security-icon">🔐</div>
                <div class="security-text">Sicherheitsprotokoll aktiviert</div>
            </div>
        </div>

        <!-- Content -->
        <div class="content">
            <!-- Greeting -->
            <div class="greeting">
                <div style="font-size: 16px; color: #ffaa66; margin-bottom: 10px;">
                    🚨 Sicherheitswarnung
                </div>
                <div class="commander-name">
                    Kommandeur {{username}}
                </div>
                <div style="font-size: 14px; color: #ffddaa; margin-top: 10px;">
                    Anfrage zur Sicherheitscode-Zurücksetzung empfangen
                </div>
            </div>

            <div class="divider"></div>

            <!-- Message Section -->
            <div class="message-section">
                <div class="message-text">
                    <strong>📡 TRANSMISSION DETAILS:</strong><br><br>
                    Wir haben eine Anfrage zur Zurücksetzung deines Sicherheitscodes erhalten.
                    Falls du diese Anfrage <strong>nicht</strong> gestellt hast, ignoriere diese Nachricht.
                    Dein aktueller Zugang bleibt unverändert.
                </div>
            </div>

            <!-- Reset Section -->
            <div class="reset-section">
                <div class="reset-title">
                    🔧 SICHERHEITSCODE ZURÜCKSETZEN
                </div>

                <p style="color: #aaddff; font-size: 14px; margin-bottom: 20px;">
                    Klicke auf den folgenden Link, um einen neuen Sicherheitscode festzulegen:
                </p>

                <a href="{{resetUrl}}" class="reset-button">
                    🔑 Neuen Code festlegen
                </a>

                <div style="margin-top: 20px; font-size: 12px; color: #99ccff;">
                    Alternativ kannst du den folgenden Token manuell eingeben:
                </div>

                <div class="token-display">
                    {{resetToken}}
                </div>
            </div>

            <!-- Timer Section -->
            <div class="timer-section">
                <div class="timer-text">
                    ⏱️ GÜLTIGKEIT: {{expirationHours}} STUNDEN
                </div>
                <div class="timer-detail">
                    Dieser Link läuft automatisch ab und kann nur einmal verwendet werden.
                </div>
            </div>

            <!-- Warning Section -->
            <div class="warning-section">
                <div class="warning-title">
                    ⚠️ SICHERHEITSHINWEISE
                </div>
                <div class="warning-text">
                    • Teile diesen Link niemals mit anderen<br>
                    • Verwende einen starken, einzigartigen Sicherheitscode<br>
                    • Bei verdächtigen Aktivitäten kontaktiere sofort den Support<br>
                    • Dieser Link funktioniert nur einmal und läuft in {{expirationHours}} Stunden ab
                </div>
            </div>

            <div class="terminal-border">
                <p style="color: #ffddaa; font-size: 13px; text-align: center;">
                    <strong>📋 SYSTEM-LOG:</strong> Reset-Request erfolgreich verarbeitet<br>
                    <em>Timestamp: {{currentDateTime}} | Session: SECURE</em>
                </p>
            </div>

            <div class="divider"></div>

            <!-- Final Message -->
            <div style="text-align: center; color: #ffddaa; font-size: 14px; line-height: 1.5;">
                <p style="margin-bottom: 15px;">
                    <strong style="color: #ff6600;">Die Sicherheit der Galaxis liegt in deinen Händen!</strong>
                </p>
                <p>
                    Solltest du Hilfe benötigen, steht dir unser Support-Team zur Verfügung.
                </p>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div class="footer-links">
                <a href="{{gameUrl}}" class="footer-link">Zum Spiel</a>
                <a href="{{gameUrl}}/help" class="footer-link">Support</a>
                <a href="{{gameUrl}}/settings" class="footer-link">Einstellungen</a>
            </div>
            <div>
                © {{currentYear}} Star Wars Universe - Sicherheitszentrum<br>
                <em>Diese Nachricht wurde automatisch generiert und ist vertraulich.</em>
            </div>
        </div>
    </div>
</body>
</html>