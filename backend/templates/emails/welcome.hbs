<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Willkommen im Star Wars Universe</title>
    <style>
        /* Reset styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Courier New', 'Monaco', 'Lucida Console', monospace;
            background: #000408;
            color: #00ffff;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: linear-gradient(135deg, #000408 0%, #000812 50%, #000204 100%);
            border: 2px solid #00ffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 0 30px rgba(0, 255, 255, 0.3);
        }

        .header {
            background: linear-gradient(90deg, #003333 0%, #004444 50%, #003333 100%);
            border-bottom: 2px solid #00ffff;
            padding: 30px 20px;
            text-align: center;
            position: relative;
        }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, #00ffff, transparent);
        }

        .title {
            font-size: 28px;
            font-weight: bold;
            text-shadow: 0 0 10px #00ffff;
            letter-spacing: 3px;
            margin-bottom: 10px;
        }

        .subtitle {
            font-size: 14px;
            color: #66ffff;
            letter-spacing: 2px;
            font-weight: normal;
        }

        .content {
            padding: 40px 30px;
        }

        .welcome-section {
            text-align: center;
            margin-bottom: 40px;
        }

        .welcome-message {
            font-size: 18px;
            color: #00dddd;
            margin-bottom: 15px;
        }

        .commander-name {
            font-size: 24px;
            color: #00ffff;
            font-weight: bold;
            text-shadow: 0 0 5px #00ffff;
            margin: 10px 0;
        }

        .divider {
            height: 1px;
            background: linear-gradient(90deg, transparent, #00ffff, transparent);
            margin: 30px 0;
        }

        .invite-codes-section {
            background: linear-gradient(135deg, #001122 0%, #002244 100%);
            border: 1px solid #00ffff;
            border-radius: 8px;
            padding: 30px;
            margin: 30px 0;
            text-align: center;
        }

        .invite-codes-title {
            font-size: 16px;
            color: #66ffff;
            margin-bottom: 20px;
            letter-spacing: 1px;
        }

        .invite-code {
            display: inline-block;
            background: #003366;
            color: #00ffff;
            font-family: 'Courier New', monospace;
            font-size: 20px;
            font-weight: bold;
            padding: 15px 20px;
            margin: 10px;
            border: 2px solid #0099ff;
            border-radius: 6px;
            letter-spacing: 3px;
            text-shadow: 0 0 8px #00ffff;
            box-shadow: 0 0 15px rgba(0, 153, 255, 0.3);
        }

        .invite-instructions {
            font-size: 14px;
            color: #99dddd;
            margin-top: 20px;
            line-height: 1.4;
        }

        .cta-section {
            text-align: center;
            margin: 40px 0;
        }

        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #00aaff 0%, #0088cc 100%);
            color: #000;
            font-family: 'Courier New', monospace;
            font-size: 16px;
            font-weight: bold;
            text-decoration: none;
            padding: 15px 30px;
            border: 2px solid #00ffff;
            border-radius: 6px;
            letter-spacing: 1px;
            text-transform: uppercase;
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.4);
            transition: all 0.3s ease;
        }

        .cta-button:hover {
            background: linear-gradient(135deg, #00ffff 0%, #00ccaa 100%);
            box-shadow: 0 0 25px rgba(0, 255, 255, 0.6);
        }

        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin: 30px 0;
        }

        .info-card {
            background: linear-gradient(135deg, #001a1a 0%, #002222 100%);
            border: 1px solid #004444;
            border-radius: 6px;
            padding: 20px;
            text-align: center;
        }

        .info-card-title {
            font-size: 14px;
            color: #00ffff;
            font-weight: bold;
            margin-bottom: 10px;
            letter-spacing: 1px;
        }

        .info-card-content {
            font-size: 12px;
            color: #99dddd;
            line-height: 1.4;
        }

        .footer {
            background: #001122;
            border-top: 1px solid #004444;
            padding: 25px;
            text-align: center;
            font-size: 12px;
            color: #666666;
        }

        .footer-links {
            margin-bottom: 15px;
        }

        .footer-link {
            color: #00aaaa;
            text-decoration: none;
            margin: 0 15px;
        }

        .footer-link:hover {
            color: #00ffff;
        }

        .terminal-border {
            border: 1px dashed #00ffff;
            background: rgba(0, 255, 255, 0.05);
            padding: 15px;
            margin: 20px 0;
            border-radius: 4px;
        }

        /* Mobile responsive */
        @media (max-width: 600px) {
            .container {
                margin: 10px;
                border-radius: 8px;
            }

            .content {
                padding: 20px 15px;
            }

            .info-grid {
                grid-template-columns: 1fr;
            }

            .invite-code {
                font-size: 16px;
                padding: 10px 15px;
                margin: 5px;
            }

            .title {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="title">STAR WARS</div>
            <div class="subtitle">UNIVERSE</div>
        </div>

        <!-- Content -->
        <div class="content">
            <!-- Welcome Section -->
            <div class="welcome-section">
                <div class="welcome-message">
                    🚀 Transmission erfolgreich empfangen
                </div>
                <div class="commander-name">
                    Kommandeur {{username}}
                </div>
                <div style="font-size: 14px; color: #99dddd; margin-top: 10px;">
                    Willkommen im galaktischen Konflikt
                </div>
            </div>

            <div class="divider"></div>

            <!-- Welcome Text -->
            <div class="terminal-border">
                <p style="color: #00dddd; margin-bottom: 15px;">
                    <strong>📡 SYSTEMSTATUS:</strong> Registrierung abgeschlossen
                </p>
                <p style="color: #99dddd; font-size: 14px; line-height: 1.5;">
                    Deine Identität wurde erfolgreich in unserem galaktischen Netzwerk registriert.
                    Du bist nun berechtigt, an den Kämpfen zwischen dem <strong>Galaktischen Imperium</strong>
                    und der <strong>Rebellen-Allianz</strong> teilzunehmen.
                </p>
            </div>

            <!-- Invite Codes Section -->
            <div class="invite-codes-section">
                <div class="invite-codes-title">
                    🔑 DEINE PERSÖNLICHEN ZUGANGSSCHLÜSSEL
                </div>

                {{#each inviteCodes}}
                <div class="invite-code">{{this}}</div>
                {{/each}}

                <div class="invite-instructions">
                    Diese Codes ermöglichen es dir, Verbündete in den Kampf zu rufen.<br>
                    Teile sie nur mit vertrauenswürdigen Rekruten!<br>
                    <em>Du findest deine Codes jederzeit in den Spieleinstellungen.</em>
                </div>
            </div>

            <!-- Call to Action -->
            <div class="cta-section">
                <a href="{{gameUrl}}" class="cta-button">
                    🎮 Zur Mission starten
                </a>
            </div>

            <!-- Info Cards -->
            <div class="info-grid">
                <div class="info-card">
                    <div class="info-card-title">🌍 ERSTE SCHRITTE</div>
                    <div class="info-card-content">
                        Besiedle deinen ersten Planeten, baue Strukturen und beginne mit der Rohstoffproduktion
                    </div>
                </div>
                <div class="info-card">
                    <div class="info-card-title">⚔️ GALAKTISCHER KRIEG</div>
                    <div class="info-card-content">
                        Schließe dich anderen Spielern an und kämpfe um die Kontrolle der Galaxis
                    </div>
                </div>
            </div>

            <div class="divider"></div>

            <!-- Final Message -->
            <div style="text-align: center; color: #99dddd; font-size: 14px; line-height: 1.5;">
                <p style="margin-bottom: 10px;">
                    <strong style="color: #00ffff;">May the Force be with you, Kommandeur!</strong>
                </p>
                <p>
                    Das Star Wars Universe Team freut sich auf epische Schlachten mit dir.
                </p>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div class="footer-links">
                <a href="{{gameUrl}}" class="footer-link">Spiel starten</a>
                <a href="{{gameUrl}}/settings" class="footer-link">Einstellungen</a>
                <a href="{{gameUrl}}/help" class="footer-link">Hilfe</a>
            </div>
            <div>
                © {{currentYear}} Star Wars Universe - Ein galaktisches Strategiespiel<br>
                <em>Diese Nachricht wurde automatisch generiert.</em>
            </div>
        </div>
    </div>
</body>
</html>